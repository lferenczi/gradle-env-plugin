apply plugin: 'maven'
apply plugin: 'groovy'
apply plugin: 'eclipse'

project.group = "org.gradle.api.plugins"

repositories {
    mavenCentral()
}

dependencies {
    compile gradleApi()
    compile localGroovy()
    
    testCompile ("junit:junit:4.12")
}
